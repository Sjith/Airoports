<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.airoports.ui"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Airoports"
        >
        <activity
            android:name=".AiroportsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name="ScheduleArriveActivity" >
        </activity>
        <activity android:name="ScheduleDeparturesActivity" >
        </activity>

        <provider
            android:name="by.airoports.provider.DBProvider"
            android:authorities="by.airoports.ui" >
        </provider>

        <activity android:name="MainMenuActivity" 
            android:clearTaskOnLaunch="true"
            android:launchMode="singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>